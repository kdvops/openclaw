apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdvops.fullname" . }}
  labels:
    {{- include "kdvops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kdvops.selectorLabels" . | nindent 4 }}
