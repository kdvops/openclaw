apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openclaw-backend
  namespace: argocd
spec:
  project: default

  sources:

    # 🔹 Source 1 → Namespace
    - repoURL: https://github.com/kdvops/openclaw.git
      targetRevision: main
      path: k8s/apps/openclaw/backend/namespace/


    # 🔹 Source 2 → Helm App
    - repoURL: https://github.com/kdvops/openclaw.git
      targetRevision: main
      path: k8s/apps/openclaw/backend/webapp
      helm:
        valueFiles:
          - values.yaml
        parameters:
          - name: fullnameOverride
            value: backend

  destination:
    server: https://kubernetes.default.svc
    namespace: openclaw

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ServerSideApply=true