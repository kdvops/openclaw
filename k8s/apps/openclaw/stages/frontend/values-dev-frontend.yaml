# Independent stage values for openclaw frontend (DEV)

image:
  repository: ghcr.io/openclaw/openclaw
  tag: latest

service:
  strategy:
    - name: active
      host: openclaw.kdvops.com
    - name: preview
      host: preview-openclaw.kdvops.com

virtualService:
  hosts:
    - openclaw.kdvops.com

ingress:
  tlsSecret: openclaw-frontend-dev-tls
  paths:
    - path: /hubs
      service: ""
      activeSuffix: active
      previewSuffix: preview
      port: 80
    - path: /
      service: ""
      activeSuffix: active
      previewSuffix: preview
      port: 80

configmap:
  data:
    APP_ENV: dev
    API_BASE_URL: /graphql

env:
  - name: TEST
    value: ""

