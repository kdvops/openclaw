# Independent stage values for openclaw frontend (PROD)

image:
  repository: ghcr.io/openclaw/openclaw
  tag: latest

service:
  strategy:
    - name: active
      host: openclaw.example.com
    - name: preview
      host: preview-openclaw.example.com

virtualService:
  hosts:
    - openclaw.example.com

ingress:
  tlsSecret: openclaw-frontend-prod-tls
  paths:
    - path: /hubs
      service: ""
      activeSuffix: active
      previewSuffix: preview
      port: 80
    - path: /
      service: ""
      activeSuffix: active
      previewSuffix: preview
      port: 80

configmap:
  data:
    APP_ENV: prod
    API_BASE_URL: /graphql

env:
  - name: TEST
    value: ""
